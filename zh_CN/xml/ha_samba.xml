<?xml version="1.0" encoding="UTF-8"?>
<chapter xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:base="ha_samba.xml" version="5.0" xml:id="cha-ha-samba">
 <title>Samba 群集</title>
 <info>
      <abstract>
        <para>
    群集 Samba 服务器提供异构网络的高可用性解决方案。本章说明了一些背景信息以及如何设置群集 Samba 服务器。
   </para>
      </abstract>
      <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
        <dm:maintainer/>
        <dm:status>编辑</dm:status>
        <dm:deadline/>
        <dm:priority/>
        <dm:translation>yes</dm:translation>
        <dm:languages/>
        <dm:release/>
        <dm:repository/>
      </dm:docmanager>
    </info>
    <sect1 xml:id="sec-ha-samba-overview">
  <title>概念概述</title>

  <para>
   Samba 使用 Trivial Database (TDB) 已经许多年了。它允许多个应用程序同时写入。为确保所有写操作都成功执行而不会彼此冲突，TDB 使用内部锁定机制。
  </para>

  <para>
   Cluster Trivial Database (CTDB) 是现有的 TDB 的小扩展。项目对 CTDB 的描述是：<quote>Samba 和其他项目用于储存临时数据的 TDB 数据库的群集实现</quote>。
  </para>

  <para>
   每个群集节点都运行本地 CTDB 守护程序。Samba 与其本地 CTDB 守护程序通讯，而非直接写入其 TDB。守护程序通过网络交换元数据，但实际的读写操作是在快速储存的本地副本上进行的。CTDB 的概念如<xref linkend="fig-ha-samba-overview"/>中所示。
  </para>

  <note>
   <title>CTDB 仅用于 Samba</title>
   <para>
    CTDB 资源代理的当前实现将 CTDB 配置为只管理 Samba。任何其他功能，包括 IP 故障转移，都应使用 Pacemaker 进行配置。
   </para>
   <para>
    CTDB 仅支持完全同类的群集。例如，群集中的所有节点都必须有相同的体系结构，不能混合 i586 与 x86_64。
   </para>
  </note>

  <figure xml:id="fig-ha-samba-overview">
   <title>CTDB 群集的结构</title>
   <mediaobject>
    <imageobject role="fo">
     <imagedata fileref="ha_samba.svg" width="80%"/>
    </imageobject>
    <imageobject role="html">
     <imagedata fileref="ha_samba.png" width="80%"/>
    </imageobject>
   </mediaobject>
  </figure>

  <para>
   群集 Samba 服务器必须共享某些数据：
  </para>

  <itemizedlist>
   <listitem>
    <para>
     将 Unix 用户和组 ID 与 Windows 用户和组关联的映射表。
    </para>
   </listitem>
   <listitem>
    <para>
     用户数据库必须在所有节点间同步。
    </para>
   </listitem>
   <listitem>
    <para>
     Windows 域中的成员服务器的连接信息必须在所有节点上都可用。
    </para>
   </listitem>
   <listitem>
    <para>
     元数据（如活动 SMB 会话、共享连接和各种锁）需在所有节点上都可用。
    </para>
   </listitem>
  </itemizedlist>

  <para>
   目标是：具有 N+1 个节点的群集 Samba 服务器比只有 N 个节点的快。一个节点不会比非群集 Samba 服务器慢。
  </para>
 </sect1>
 <sect1 xml:id="sec-ha-samba-basicconf">
  <title>基本配置</title>

  <remark>toms 2014-02-28: From "Tim": "I want to see if we can change this 
    to match what's described in fate#316336 (i.e. separate samba and winbind resources).  
    This is largely a testing/validation effort."</remark>

  <note>
   <title>已更改的配置文件</title>
   <para>
    CTDB 资源代理会自动更改 <filename>/etc/sysconfig/ctdb</filename>。使用 <command>crm ra</command> <option>info CTDB</option> 列出可为 CTDB 资源指定的所有参数。
   </para>
  </note>

  <para>
   要设置群集 Samba 服务器，请按如下操作：
  </para>

  <procedure xml:id="pro-ha-samba-basicconf">
   <title>设置基本的群集 Samba 服务器</title>
   <step>
    <para>
     准备群集：
    </para>
    <substeps performance="required">
     <step>
      <para>
       继续操作之前，请确保以下包都已安装：<systemitem class="resource">ctdb</systemitem>、<systemitem class="resource">tdb-tools</systemitem> 和 <systemitem class="resource">samba</systemitem>（<literal>smb</literal> 和 <literal>nmb</literal> 资源需要这些包）。
      </para>
     </step>
     <step>
      <para>
       根据本指南的<xref linkend="part-config"/>中所述配置您的群集（Pacemaker、OCFS2）。
      </para>
     </step>
     <step>
      <para>
       配置共享文件系统（如 OCFS2）并将其装入，例如装入到 <filename>/srv/clusterfs</filename> 上。有关详细信息，请参见 <xref linkend="cha-ha-ocfs2"/>。
      </para>
     </step>
     <step>
      <para>
       要打开 POSIX ACL，请启用它：
      </para>
      <itemizedlist>
       <listitem>
        <para>
         对新的 OCFS2 文件系统，使用：
        </para>
<screen><prompt role="root">root # </prompt><command>mkfs.ocfs2</command> --fs-features=xattr ...</screen>
       </listitem>
       <listitem>
        <para>
         对现有 OCFS2 文件系统，使用：
        </para>
<screen><prompt role="root">root # </prompt><command>tunefs.ocfs2</command> --fs-feature=xattr <replaceable>DEVICE</replaceable></screen>
        <para>
         确保在文件系统资源中指定了 <option>acl</option> 选项。按如下方式使用 <command>crm</command> 外壳：
        </para>
<screen><prompt role="custom">crm(live)configure# </prompt><command>primitive</command> ocfs2-3 ocf:heartbeat:Filesystem params options="acl" ...</screen>
       </listitem>
      </itemizedlist>
     </step>
     <step>
      <para>
       确保服务 <systemitem class="service">ctdb</systemitem>、<systemitem class="service">smb</systemitem> 和 <systemitem class="service">nmb</systemitem> 已禁用：
      </para>
<screen><prompt role="root">root # </prompt><command>systemctl</command> disable ctdb
<prompt role="root">root # </prompt><command>systemctl</command> disable smb
<prompt role="root">root # </prompt><command>systemctl</command> disable nmb</screen>
     </step>
     <step>
      <para>
       在所有节点上打开防火墙的端口 <literal>4379</literal>。这是为了使 CTDB 能够与其他群集节点通讯。
      </para>
     </step>
    </substeps>
   </step>
   <step>
    <para>
     在共享文件系统上为 CTDB 锁定创建一个目录：
    </para>
<screen><prompt role="root">root # </prompt><command>mkdir</command> -p /srv/clusterfs/samba/</screen>
   </step>
   <step>
    <para>
     在 <filename>/etc/ctdb/nodes</filename> 中插入包含群集中每个节点的所有私用 IP 地址的所有节点：
    </para>
<screen>192.168.1.10
192.168.1.11</screen>
   </step>
   <step>
    <para>
     配置 Samba。在 <filename>/etc/samba/smb.conf</filename> 的 <literal>[global]</literal> 部分中添加以下行。使用所选的主机名取代“CTDB-SERVER”（群集中的所有节点将显示为一个此名称的大节点，以方便操作）：
    </para>
<screen>[global]
    # ...
    # settings applicable for all CTDB deployments
    netbios name = CTDB-SERVER
    clustering = yes
    idmap config * : backend = tdb2
    passdb backend = tdbsam
    ctdbd socket = /var/lib/ctdb/ctdb.socket
    # settings necessary for CTDB on OCFS2
    fileid:algorithm = fsid
    vfs objects = fileid
    # ...</screen>
   </step>
   <step>
    <para>
     使用 <command>csync2</command> 将配置文件复制到您的所有节点：
    </para>
<screen><prompt role="root">root # </prompt><command>csync2</command> <option>-xv</option></screen>
    <para>
     有关详细信息，请参见 <xref linkend="pro-ha-installation-setup-csync2-start"/>。
    </para>
   </step>
   <step>
    <para>
     将 CTDB 资源添加到群集：
    </para>

<screen><prompt role="root">root # </prompt><command>crm</command> configure
<prompt role="custom">crm(live)configure# </prompt><command>primitive</command> ctdb ocf:heartbeat:CTDB params \
    ctdb_manages_winbind="false" \ 
    ctdb_manages_samba="false" \
    ctdb_recovery_lock="/srv/clusterfs/samba/ctdb.lock" \
    ctdb_socket="/var/lib/ctdb/ctdb.socket" \ 
      op monitor interval="10" timeout="20" \
      op start interval="0" timeout="90" \
      op stop interval="0" timeout="100"
<prompt role="custom">crm(live)configure# </prompt><command>primitive</command> nmb systemd:nmb \
    op start timeout="60" interval="0" \
    op stop timeout="60" interval="0" \
    op monitor interval="60" timeout="60"
<prompt role="custom">crm(live)configure# </prompt><command>primitive</command> smb systemd:smb \
    op start timeout="60" interval="0" \
    op stop timeout="60" interval="0" \
    op monitor interval="60" timeout="60"
<prompt role="custom">crm(live)configure# </prompt><command>group</command> ctdb-group ctdb nmb smb
<prompt role="custom">crm(live)configure# </prompt><command>clone</command> ctdb-clone ctdb-group meta interleave="true"
<prompt role="custom">crm(live)configure# </prompt><command>colocation</command> ctdb-with-clusterfs inf: ctdb-clone c-clusterfs
<prompt role="custom">crm(live)configure# </prompt><command>order</command> clusterfs-then-ctdb inf: c-clusterfs ctdb-clone
<prompt role="custom">crm(live)configure# </prompt><command>commit</command></screen>
   </step>
   <step>
    <para>
     添加群集 IP 地址：
    </para>
<screen><prompt role="custom">crm(live)configure# </prompt><command>primitive</command> ip ocf:heartbeat:IPaddr2 params ip=192.168.2.222 \
    unique_clone_address="true" \
    op monitor interval="60" \
    meta resource-stickiness="0"
<prompt role="custom">crm(live)configure# </prompt><command>clone</command> ip-clone ip \
    meta interleave="true" clone-node-max="2" globally-unique="true"
<prompt role="custom">crm(live)configure# </prompt><command>colocation</command> col-with-ctdb 0: ip-clone ctdb-clone
<prompt role="custom">crm(live)configure# </prompt><command>order</command> o-with-ctdb 0: ip-clone ctdb-clone
<prompt role="custom">crm(live)configure# </prompt><command>commit</command></screen>
    <para>
     如果 <literal>unique_clone_address</literal> 设置为 <literal>true</literal>，IPaddr2 资源代理将向指定的地址添加一个克隆 ID，从而导致出现三个不同的 IP 地址。这些地址通常是不需要的，但有助于实现负载平衡。有关此主题的更多信息，请参见<xref linkend="sec-ha-lb-lvs"/>。
    </para>
   </step>
   <step>
    <para>
     提交更改：
    </para>
<screen><prompt role="custom">crm(live)configure# </prompt><command>commit</command></screen>
   </step>
   <step>
    <para>
     检查结果：
    </para>
<screen><prompt role="root">root # </prompt><command>crm</command> status
Clone Set: base-clone [dlm]
     Started: [ factory-1 ]
     Stopped: [ factory-0 ]
Clone Set: c-clusterfs [clusterfs]
     Started: [ factory-1 ]
     Stopped: [ factory-0 ]
 Clone Set: ctdb-clone [ctdb-group]
     Started: [ factory-1 ]
     Started: [ factory-0 ]
 Clone Set: ip-clone [ip] (unique)
     ip:0       (ocf::heartbeat:IPaddr2):       Started factory-0
     ip:1       (ocf::heartbeat:IPaddr2):       Started factory-1</screen>
   </step>
   <step>
    <para>
     从客户端计算机进行测试。在 Linux 客户端上运行以下命令，以检查能否从系统复制文件以及将文件复制到系统：
    </para>
<screen><prompt role="root">root # </prompt><command>smbclient</command> <option>//192.168.2.222/myshare</option></screen>
   </step>

  </procedure>
 </sect1>
 <sect1 xml:id="sec-ha-samba-ad">
  <title>加入 Active Directory 域</title>

  <remark>toms 2013-04-02: Needs changes</remark>

  <para>
   Active Directory (AD) 是 Windows Server 系统的一项目录服务。
  </para>

  <para>
   下列说明概述了如何将 CTDB 群集加入到 Active Directory 域：
  </para>

  <procedure xml:id="pro-ha-samba-ad-join">
   <step>
    <para>
     按照<xref linkend="pro-ha-samba-basicconf"/>中所述创建 CTDB 资源。
    </para>
   </step>
   <step>
    <para>
     安装 <systemitem class="resource">samba-winbind</systemitem> 包。
    </para>
   </step>
   <step>
    <para>
     禁用 <systemitem class="service">winbind</systemitem> 服务：
    </para>
<screen><prompt role="root">root # </prompt><command>systemctl</command> disable winbind</screen>
   </step>
   <step>
    <para>
     定义 winbind 群集资源：
    </para>

<screen><prompt role="root">root # </prompt><command>crm</command> configure
<prompt role="custom">crm(live)configure# </prompt><command>primitive</command> winbind systemd:winbind \
    op start timeout="60" interval="0" \
    op stop timeout="60" interval="0" \
    op monitor interval="60" timeout="60"
<prompt role="custom">crm(live)configure# </prompt><command>commit</command></screen>
   </step>
   <step>
    <para>
     编辑 <literal>ctdb-group</literal>，并在 <literal>nmb</literal> 和 <literal>smb</literal> 资源之间插入 <literal>winbind</literal>：
    </para>
<screen><prompt role="custom">crm(live)configure# </prompt><command>edit</command> ctdb-group</screen>
    <para>
     保存更改，然后使用 <keycombo> <keycap>:</keycap>
     <keycap>w</keycap> </keycombo> (<command>vim</command>) 关闭编辑器。
    </para>
   </step>
   <step>
    <para>
     有关如何设置 Active Directory 域的说明，请参见 Windows Server 文档。在此示例中，使用以下参数：
    </para>
    <informaltable>
     <tgroup cols="2">
      <tbody>
       <row>
        <entry>
         <para>
          AD 和 DNS 服务器
         </para>
        </entry>
        <entry>
<screen>win2k3.2k3test.example.com</screen>
        </entry>
       </row>
       <row>
        <entry>
         <para>
          AD 域
         </para>
        </entry>
        <entry>
<screen>2k3test.example.com</screen>
        </entry>
       </row>
       <row>
        <entry>
         <para>
          群集 AD 成员 NETBIOS 名称
         </para>
        </entry>
        <entry>
<screen>CTDB-SERVER</screen>
        </entry>
       </row>
      </tbody>
     </tgroup>
    </informaltable>

   </step>
   <step>
    <para>
     <xref linkend="pro-ha-samba-config-join-ad"/>
    </para>
   </step>
  </procedure>

  <para>
   最后，将群集加入 Active Directory 服务器：
  </para>

  <procedure xml:id="pro-ha-samba-config-join-ad">
   <title>加入 Active Directory</title>
   <step>
    <para>
     请确保 Csync2 的配置中包含下列文件，才可在所有群集主机上进行安装：
    </para>
<screen>/etc/samba/smb.conf
/etc/security/pam_winbind.conf
/etc/krb5.conf
/etc/nsswitch.conf
/etc/security/pam_mount.conf.xml
/etc/pam.d/common-session</screen>
    <para>
     您也可以为此任务使用 YaST 的<guimenu>配置 Csync2</guimenu> 模块，请参见<xref linkend="sec-ha-installation-setup-csync2"/>。
    </para>
   </step>
   <step>
    <para>
     运行 YaST 并从<guimenu>网络服务</guimenu>项中打开 <guimenu>Windows 域成员资格</guimenu>模块。
    </para>
   </step>
   <step>
    <para>
     输入域或工作组设置然后单击<guimenu>确定</guimenu>完成设置。
    </para>
   </step>
  </procedure>
 </sect1>
 <sect1 xml:id="sec-ha-samba-testing">
  <title>调试和测试群集 Samba</title>

  <para>
   要调试群集 Samba 服务器，可使用以下作用于不同级别的工具：
  </para>

  <variablelist>
   <varlistentry>
    <term><command>ctdb_diagnostics</command>
    </term>
    <listitem>
     <para>
      运行此工具可诊断群集 Samba 服务器。详细的调试消息应有助于您跟踪任何可能存在的问题。
     </para>
     <para>
      <command>ctdb_diagnostics</command> 命令可搜索以下文件，这些文件必须在所有节点上都可用：
     </para>
<screen>/etc/krb5.conf
/etc/hosts
/etc/ctdb/nodes
/etc/sysconfig/ctdb
/etc/resolv.conf
/etc/nsswitch.conf
/etc/sysctl.conf
/etc/samba/smb.conf
/etc/fstab
/etc/multipath.conf
/etc/pam.d/system-auth
/etc/sysconfig/nfs
/etc/exports
/etc/vsftpd/vsftpd.conf</screen>
     <para>
      如果文件 <filename>/etc/ctdb/public_addresses</filename> 和 <filename>/etc/ctdb/static-routes</filename> 存在，它们也会被检查。
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><command>ping_pong</command>
    </term>
    <listitem>
     <para>
      检查文件系统是否支持 CTDB 使用 <command>ping_pong</command>。它会对群集文件系统执行一致性和性能之类的特定测试（请参见 <link xlink:href="http://wiki.samba.org/index.php/Ping_pong"/>），从而给出群集在高负载下将会表现如何的一些指示。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><command>send_arp</command> 工具和 <systemitem class="resource">SendArp</systemitem> 资源代理</term>
    <listitem>
     <para>
      <systemitem class="resource">SendArp</systemitem> 资源代理位于 <filename>/usr/lib/heartbeat/send_arp</filename>（或 <filename>/usr/lib64/heartbeat/send_arp</filename>）。<command>send_arp</command> 工具发出免费的 ARP（Address Resolution Protocol，地址解析协议）包，可用于更新其他计算机的 ARP 表。它可以帮助确定故障转移过程之后的通讯问题。如果节点显示 Samba 的群集 IP 地址，但您却无法连接到该节点或 ping 它，请使用 <command>send_arp</command> 命令测试节点是否只需要更新 ARP 表。
     </para>
     <para>
      有关更多信息，请参见 <link xlink:href="http://wiki.wireshark.org/Gratuitous_ARP"/>。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>

  <para>
   要测试群集文件系统的某些方面，请如下继续操作：
  </para>

  <procedure>
   <title>测试群集文件系统的一致性和性能</title>
   <step>
    <para>
     在一个节点上启动命令 <command>ping_pong</command>，将占位符 <replaceable>N</replaceable> 替换为节点数 + 1。文件 <filename><replaceable>ABSPATH</replaceable>/data.txt</filename> 在共享存储区中提供，因此在所有节点（<replaceable>ABSPATH</replaceable> 表示绝对路径）上都可以访问到：
    </para>
<screen><command>ping_pong</command> <replaceable>ABSPATH</replaceable>/data.txt <replaceable>N</replaceable></screen>
    <para>
     应该会得到很高的锁定率，因为只运行一个节点。如果程序不打印锁定率，请替换群集文件系统。
    </para>
   </step>
   <step>
    <para>
     使用相同的参数在另一个节点上启动 <command>ping_pong</command> 的第二个副本。
    </para>
    <para>
     应该会看到锁定率急剧下降。如果以下任意情况适用于群集文件系统，请替换它：
    </para>
    <itemizedlist>
     <listitem>
      <para>
       <command>ping_pong</command> 不打印每秒锁定率，
      </para>
     </listitem>
     <listitem>
      <para>
       两个实例中的锁定率并非几乎相等,
      </para>
     </listitem>
     <listitem>
      <para>
       启动第二个实例后锁定率未下降。
      </para>
     </listitem>
    </itemizedlist>
   </step>
   <step>
    <para>
     启动 <command>ping_pong</command> 的第三个副本。添加另一个节点，注意锁定率的变化。
    </para>
   </step>
   <step>
    <para>
     逐个终止 <command>ping_pong</command> 命令。应该观察到锁定率上升，直到回到单一节点的情况。如果没有看到预期行为，请参见<xref linkend="cha-ha-ocfs2"/> 中的详细信息。
    </para>
   </step>
  </procedure>
 </sect1>
 <sect1 xml:id="sec-ha-samba-moreinfo">
  <title>更多信息</title>

  <itemizedlist>
   <listitem>
    <para>
     <link xlink:href="http://linux-ha.org/wiki/CTDB_(resource_agent)"/>
    </para>
   </listitem>
   <listitem>
    <para>
     <link xlink:href="http://wiki.samba.org/index.php/CTDB_Setup"/>
    </para>
   </listitem>
   <listitem>
    <para>
     <link xlink:href="http://ctdb.samba.org"/>
    </para>
   </listitem>
   <listitem>
    <para>
     <link xlink:href="http://wiki.samba.org/index.php/Samba_%26_Clustering"/>
    </para>
   </listitem>
  </itemizedlist>
 </sect1>
</chapter>
